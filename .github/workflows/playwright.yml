name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-playwright
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Install Playwright Browsers
      run: playwright install --with-deps
    - name: Run Playwright tests
      run: pytest
    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
    - name: Pós-teste - Validação Blackbox (Placeholder)
      if: always()
      run: |
        echo "Aguardando 1 minuto para o deploy propagar..."
        sleep 60
        echo "TODO: Incluir a chamada de API para validação blackbox do deploy."
        # curl -X POST https://api.futura.com/webhook -d '{"status": "success"}'
